<div class="container-fluid">
    <div class="row align-items-center justify-content-center min-vh-100">
        <div class="col-sm-4">
            <div class="card">
            <div class="card-header">
                <h5 class="card-title text-center mb-0">Editar Vehiculo</h5>
            </div>
                <div class="card-body mb-3">
                    <%= render "form", vehiculo: @vehiculo %>
                </div>
                <div class="card-footer d-md-flex justify-content-md-end">
                    <%= link_to 'Volver', vehiculo_path(@vehiculo), class: "btn btn-secondary" %>
                </div>
            </div>
        </div>
    </div>
</div>